# 企业 AI 审计与可观测性体系
## 面向 Agent 平台的日志、追踪、监控与责任回溯设计

> **文档类型**：专题设计白皮书  
> **版本**：1.0  
> **发布日期**：2026-04-02  
> **适用对象**：平台运维、SRE、安全审计、合规负责人、AI 平台架构师、质量团队  
> **核心主题**：操作留痕、调用链追踪、风险告警、质量回放、合规审计

### 摘要
企业 AI 平台一旦进入真实业务流程，最怕的不是模型偶尔答错，而是出问题后无法回答：**它为什么这么做、调用了哪些工具、访问了哪些数据、是谁批准的、哪个版本导致的、是否还会再次发生**。这本质上是审计与可观测性问题。

本文提出一套适用于企业 Agent / Copilot 平台的审计与可观测性体系，从事件模型、日志分层、调用链追踪、指标监控、风险告警到回放复盘，构建能够支撑安全治理、运营优化、质量改进和高合规审查的统一框架。

---

## 一、为什么企业 AI 不能只做普通日志

传统应用日志通常关注：
- 接口调用是否成功
- 响应时间是否正常
- 异常堆栈是什么

但在 AI 平台中，这远远不够，因为还需要解释：
- 用户最初输入了什么意图
- 模型看到了哪些上下文
- 模型为什么选择某个工具
- 权限是如何被判定通过的
- 工具参数是否被改写或裁剪
- 输出是否经过人工确认
- 最终结果是否进入了业务系统

这意味着企业 AI 的可观测性必须覆盖 **语义决策链 + 权限决策链 + 工具执行链 + 业务影响链**。

---

## 二、总体目标

企业 AI 审计与可观测性体系建议实现五个目标：

1. **可追踪**：还原一次任务从请求到执行的完整链路  
2. **可定位**：快速定位问题出在模型、上下文、权限、工具还是外部系统  
3. **可告警**：及时发现越权、异常高频、敏感数据暴露、失败重试等风险  
4. **可复盘**：支持事后回放与根因分析  
5. **可优化**：为提示词优化、工具改造、知识治理和成本治理提供依据

---

## 三、总体架构

```text
┌────────────────────────────────────────────────────────────┐
│                      交互与任务层                           │
│ 用户请求 / Agent 任务 / Workflow / Multi-Agent             │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                     事件采集与标准化层                       │
│ Prompt Event / Tool Event / Auth Event / Output Event      │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                     日志与追踪存储层                         │
│ 日志库 / Trace Store / Metrics / Audit Archive             │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                     分析与告警层                             │
│ SIEM / APM / Risk Rules / Cost Analytics / QA Dashboard    │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                     运营与审计使用层                         │
│ 安全审计 / SRE / 平台运维 / QA / 合规 / 管理层              │
└────────────────────────────────────────────────────────────┘
```

---

## 四、建议采集的六类核心事件

### 4.1 会话事件（Session Event）
记录谁在何时发起了什么任务。

关键字段：
- sessionId
- userId
- role
- department
- clientType
- startTime / endTime
- requestChannel

### 4.2 提示与上下文事件（Prompt & Context Event）
记录模型接收了哪些输入与上下文来源。

关键字段：
- promptTemplateVersion
- systemPromptVersion
- memorySource
- knowledgeSource
- attachedFiles
- contextTokens
- redactionApplied

### 4.3 权限决策事件（Authorization Event）
记录某个工具调用为何被允许或拒绝。

关键字段：
- toolName
- actorId
- delegatedUserId
- policyMatched
- riskLevel
- approvalRequired
- decision
- decisionReason

### 4.4 工具执行事件（Tool Execution Event）
记录工具的输入参数、执行状态和返回结果摘要。

关键字段：
- toolCallId
- toolName
- normalizedArgs
- startTime / endTime
- resultStatus
- errorCode
- rowsAffected
- confirmationMode

### 4.5 输出事件（Output Event）
记录给用户或业务系统的最终输出。

关键字段：
- outputType
- outputSummary
- sensitiveContentDetected
- humanReviewed
- deliveryChannel
- downstreamWriteOccurred

### 4.6 风险与异常事件（Risk / Exception Event）
记录越权尝试、频繁失败、批量敏感访问、异常绕过审批等。

关键字段：
- eventType
- severity
- impactedScope
- retryCount
- anomalyScore
- escalatedTo

---

## 五、推荐的日志分层模型

### L1：访问日志
用于回答“谁在什么时候使用了系统”。

### L2：任务日志
用于回答“用户发起了什么任务、系统完成情况如何”。

### L3：决策日志
用于回答“模型如何决策、使用了哪些上下文与策略”。

### L4：工具日志
用于回答“调用了哪些工具、参数是什么、成功与否”。

### L5：业务影响日志
用于回答“是否影响了业务数据、写入了哪些系统、后果是什么”。

### L6：审计归档日志
用于回答“在审计、调查和合规检查中如何长期保留证据”。

---

## 六、调用链追踪设计建议

企业 AI 建议采用 Trace ID 串联完整调用链：

```text
User Request
  └─ Session Trace
      └─ Agent Turn Trace
          ├─ Context Load Span
          ├─ Policy Decision Span
          ├─ Tool Call Span
          ├─ Approval Span
          └─ Output Span
```

每个 Span 建议记录：
- traceId / spanId / parentSpanId
- start / end timestamp
- latency
- actor
- input summary
- output summary
- status
- error category

这样做的价值在于：
- 能快速定位性能瓶颈
- 能重放错误任务
- 能分析多 Agent 协作链
- 能支持根因分析与责任界定

---

## 七、监控指标体系建议

### 7.1 可用性指标
- 请求成功率
- 工具调用成功率
- 平均响应时长
- 高风险动作审批完成率

### 7.2 安全与治理指标
- 越权尝试次数
- 敏感数据访问次数
- 被阻断的高风险调用数
- Break-Glass 使用次数

### 7.3 质量指标
- 人工退回率
- 用户二次追问率
- 输出纠错率
- 工具选择错误率

### 7.4 成本指标
- 单任务 Token 成本
- 各模型调用占比
- 工具执行成本
- 高成本任务 Top N

### 7.5 运营指标
- 活跃用户数
- 任务完成率
- 各业务场景使用热度
- 低价值高频任务识别

---

## 八、风险告警规则建议

建议至少内建以下规则：

1. **短时间内连续调用高风险工具**  
2. **低权限用户尝试访问高敏数据**  
3. **未经确认即出现业务写入动作**  
4. **审批链缺失但出现高风险执行**  
5. **敏感字段外发或导出超过阈值**  
6. **提示模板或策略版本突变后异常率飙升**  
7. **特定工具失败重试次数异常增加**  
8. **夜间或非常规地点的大批量调用**

告警设计建议遵循：
- 规则可配置
- 严重级别可分级
- 支持自动抑制噪音
- 可关联 Trace 回溯详情
- 可直接联动事件响应流程

---

## 九、审计视角下的关键保留字段

为满足审计、调查和合规要求，建议长期保留以下信息：

- 用户身份与代理身份
- Prompt 模板版本
- 系统提示版本
- 知识库版本 / 文档版本
- 工具版本与接口版本
- 权限决策结果与依据
- 审批记录与审批人
- 输出摘要与业务影响摘要
- 事件时间戳与 Trace ID

对于高合规行业，还应额外保留：
- 验证状态
- 发布版本
- 变更单号
- CAPA / 偏差关联编号

---

## 十、与安全与运维平台的协同

企业 AI 的审计与可观测性应尽量接入现有平台：

- **SIEM**：统一安全事件监控与关联分析
- **APM / Observability 平台**：统一性能、可用性、依赖关系监控
- **ITSM / 工单平台**：异常处理与变更流转
- **数据审计平台**：敏感字段访问、导出与共享控制
- **合规平台**：审计归档、证据导出、稽查支撑

---

## 十一、制药 / GxP 场景补充建议

在制药行业，审计不仅是安全要求，更是质量要求。建议额外做到：

1. 对 GxP 相关 Agent 建立独立审计域  
2. 对电子记录、电子签名、受控文件建立专项日志模型  
3. 对知识来源、参考文档版本、验证状态联动记录  
4. 对提示词、工具、模型版本变更建立正式变更控制与审批  
5. 对关键任务支持完整回放和审计证据导出

---

## 十二、建设路线建议

### 第一阶段：从“可看见”开始
- 完成最基础的访问日志和工具日志采集
- 建立统一 Trace ID
- 搭建基础运营看板

### 第二阶段：从“能定位”升级到“能治理”
- 增加权限决策事件
- 增加风险告警规则
- 接入 SIEM 与 APM

### 第三阶段：从“能治理”升级到“能复盘优化”
- 支持任务回放
- 建立质量指标与改进闭环
- 支持按部门、场景、工具、版本分析

### 第四阶段：形成审计与合规体系
- 完成高风险任务证据归档
- 输出标准审计报表
- 形成跨安全、运维、合规、质量的联合治理机制

---

## 十三、结语

企业 AI 平台的日志，不应只是“系统有没有报错”的日志，而应成为一套覆盖 **责任追踪、风险识别、质量改进、性能监控、合规证明** 的基础设施。

如果说权限体系决定了“AI 能不能做”，那么审计与可观测性体系决定了“AI 做了之后企业是否还敢继续让它做”。
