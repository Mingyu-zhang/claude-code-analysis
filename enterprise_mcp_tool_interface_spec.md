# 企业级 MCP / AI 工具接口规范
## 面向企业 AI 平台的标准化工具接入设计

> **文档类型**：接口规范白皮书  
> **版本**：1.1  
> **发布日期**：2026-04-02  
> **适用对象**：平台架构师、集成架构师、后端负责人、工具平台研发团队、安全治理团队  
> **核心主题**：工具接入标准、元数据规范、统一鉴权、统一审计、生命周期管理

### 摘要
本文提出一套适用于企业 AI 平台的 MCP / Tool 接口规范，目标是把分散的业务系统能力以统一协议方式暴露给 Agent，形成标准化、可治理、可审计、可扩展的工具接入体系。重点解决工具描述不统一、鉴权分散、日志缺失和生命周期管理薄弱等常见问题。

### 建议阅读方式
- 如果你关注 **企业 AI 工具接入标准**，可优先阅读本报告
- 如果你正在做 **平台总体架构设计**，建议与《企业 AI 平台总体架构设计》配合阅读
- 如果你要补齐 **权限与审计链路**，建议继续阅读权限模型与审计可观测性专题

---

## 一、文档目标


本文提出一套企业级 MCP / AI 工具接口规范，用于指导企业将内部系统能力标准化地接入 AI 平台，使 Agent 能够以统一方式调用查询、分析、审批和执行类工具。

目标是解决企业 AI 落地中最常见的问题：
- 工具接口五花八门，难以治理
- 鉴权逻辑分散，难以统一控制
- 日志与审计缺失，难以追溯
- Agent 对系统能力的调用方式缺乏统一契约

---

## 二、设计原则

1. **协议统一**：所有工具按统一描述方式暴露给 Agent
2. **鉴权统一**：所有工具调用经过统一身份与策略控制
3. **审计统一**：所有调用留痕可追溯
4. **语义清晰**：工具描述必须可被模型准确理解
5. **最小权限**：工具默认只开放最低必要能力
6. **可扩展性**：支持新增工具、新增系统、新增版本平滑演进

---

## 三、逻辑架构

```
┌────────────────────────────────────────────────────────┐
│                     Agent / Copilot                     │
└──────────────────────────┬─────────────────────────────┘
                           │ 工具调用请求
┌──────────────────────────▼─────────────────────────────┐
│                    Tool Gateway / MCP Hub              │
│ 元数据注册 / 身份映射 / 权限校验 / 审计留痕 / 路由转发   │
└──────────────────────────┬─────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌────────────────┐
│ ERP Tool Set  │  │ LIMS Tool Set │  │ OA / CRM Tools │
└───────────────┘  └───────────────┘  └────────────────┘
```

---

## 四、工具元数据规范

每个工具至少应定义以下元数据：

```json
{
  "name": "lims.sample.query",
  "displayName": "查询样品信息",
  "description": "根据样品编号查询样品基础信息、状态和关联批次信息",
  "category": "query",
  "system": "LIMS",
  "riskLevel": "medium",
  "version": "1.0.0",
  "inputSchema": {},
  "outputSchema": {},
  "authPolicy": "user-delegated",
  "auditLevel": "full",
  "idempotent": true,
  "timeoutSeconds": 15
}
```

### 字段说明
- `name`：全局唯一工具名称，建议采用 `系统.域.动作` 命名法
- `displayName`：面向用户和运维人员展示的名称
- `description`：给模型理解用途的自然语言描述
- `category`：query / analysis / submit / approval / automation
- `riskLevel`：low / medium / high / critical
- `authPolicy`：鉴权模式
- `auditLevel`：none / basic / full
- `idempotent`：是否幂等

---

## 五、输入输出规范

### 5.1 输入参数要求
- 必须结构化
- 必须定义字段类型和必填项
- 必须对枚举值进行约束
- 对高风险字段应增加业务校验

### 5.2 输出参数要求
- 优先结构化 JSON 输出
- 必须提供状态码与错误信息
- 高风险结果应包含来源与时间戳
- 如为建议型结果，应标识“建议”属性

示例：

```json
{
  "success": true,
  "data": {
    "sampleId": "S20260402001",
    "status": "IN_TESTING",
    "batchNo": "B24040201"
  },
  "traceId": "trace-20260402-0001",
  "timestamp": "2026-04-02T13:00:00+08:00"
}
```

---

## 六、鉴权与权限规范

### 6.1 推荐鉴权模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| user-delegated | 继承用户身份执行 | 大多数企业场景 |
| service-account | 平台服务身份执行 | 后台批处理 |
| approval-gated | 先审批后执行 | 高风险动作 |
| dual-control | 双人复核后执行 | 关键业务动作 |

### 6.2 权限控制原则
- Agent 不得天然拥有超级管理员权限
- 工具权限应基于角色、资源、动作和上下文综合判断
- 高风险工具默认不向普通 Agent 暴露
- 执行动作应支持事前校验与事后审计

---

## 七、风险分级规范

| 风险等级 | 含义 | 控制建议 |
|---------|------|---------|
| Low | 低风险查询 | 自动放行 + 基础审计 |
| Medium | 一般分析与查询 | 权限校验 + 完整日志 |
| High | 提交、变更、审批类动作 | 风险校验 + 人工确认 |
| Critical | 关键业务、关键记录或关键配置变更 | 双重审批 + 强审计 + 回退方案 |

---

## 八、审计与可观测性规范

每次工具调用至少记录：
- 调用人
- Agent 标识
- 工具名称与版本
- 输入参数摘要
- 输出结果摘要
- 风险等级
- 审批记录
- traceId
- 耗时
- 是否成功

建议接入统一日志与监控体系：
- 调用成功率
- 平均响应时间
- 高风险调用频次
- 审批通过率
- 工具级错误率

---

## 九、错误码规范

建议建立统一错误码体系：

| 错误码 | 含义 |
|-------|------|
| TOOL-400 | 输入参数非法 |
| TOOL-401 | 未认证 |
| TOOL-403 | 无权限 |
| TOOL-404 | 资源不存在 |
| TOOL-409 | 状态冲突 |
| TOOL-429 | 频率受限 |
| TOOL-500 | 系统内部错误 |
| TOOL-503 | 下游系统不可用 |

错误返回示例：

```json
{
  "success": false,
  "errorCode": "TOOL-403",
  "message": "当前身份无权访问该样品信息",
  "traceId": "trace-20260402-0002"
}
```

---

## 十、工具生命周期管理

工具不应只是“接入”，还必须被管理。

建议生命周期：
1. 需求评审
2. 风险评估
3. 设计与开发
4. 测试与验证
5. 注册上线
6. 运行监控
7. 版本迭代
8. 下线归档

---

## 十一、命名与版本管理建议

### 11.1 命名规则
建议统一命名：
`<system>.<domain>.<action>`

示例：
- `erp.purchase.query`
- `lims.sample.query`
- `oa.workflow.submit`
- `qms.deviation.createDraft`

### 11.2 版本规则
采用语义化版本：
- Major：不兼容变更
- Minor：兼容性增强
- Patch：缺陷修复

---

## 十二、落地建议

1. 先选 10-20 个高频、低风险工具标准化
2. 建立统一工具注册中心
3. 建立统一权限与审计网关
4. 对高风险工具建立审批门禁
5. 将工具规范纳入企业 AI 治理制度中

---

## 十三、总结

企业级 AI 工具体系的核心不是“把接口给模型”，而是“把企业能力以可治理、可审计、可扩展的方式交给模型”。

统一的 MCP / 工具接口规范，是企业 AI 从试点走向规模化的基础设施。
