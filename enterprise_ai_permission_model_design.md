# 企业 AI 权限模型设计
## 面向 Agent 平台的身份、授权与最小权限控制体系

> **文档类型**：专题设计白皮书  
> **版本**：1.0  
> **发布日期**：2026-04-02  
> **适用对象**：CIO、CISO、安全负责人、IAM 架构师、AI 平台架构师、合规团队  
> **核心主题**：用户身份映射、Agent 授权边界、工具级权限控制、审批门控、最小权限模型

### 摘要
企业在建设 AI Copilot / Agent 平台时，最容易低估的不是模型效果，而是权限治理复杂度。传统应用系统的权限大多围绕“人访问系统”展开，而 Agent 场景则变成“人委托 AI 访问多个系统并可能执行动作”。这会引入角色代理、上下文继承、工具粒度授权、风险分级审批、会话级临时授权等一系列新问题。

本文提出一套企业 AI 权限模型设计方法，核心目标是让 Agent 的每一次读取、分析、生成、提交和执行，都能够回答清楚五个问题：**谁发起、代表谁、访问什么、为何允许、如何追溯**。

---

## 一、为什么 Agent 时代必须重构权限模型

传统权限模型多基于以下假设：
- 用户直接登录系统
- 操作对象相对单一
- 权限粒度主要是菜单、页面、按钮、数据行
- 执行动作由人类显式触发

而在 Agent 场景下，这些假设会被打破：

1. **调用链变长**：用户 → AI 平台 → Tool Gateway → 业务系统  
2. **操作边界变模糊**：同一个问题可能触发多个系统查询与联动  
3. **语义驱动替代显式点击**：用户说一句自然语言，系统可能生成多步动作  
4. **委托关系变复杂**：Agent 既像助手，也像代理执行者  
5. **风险累积更快**：单步看似低风险，多步串联后可能形成高风险结果

因此，企业 AI 权限设计不能简单复用原有菜单权限，而需要建立“**身份映射 + 权限分层 + 风险门控 + 审计回放**”的一体化模型。

---

## 二、权限模型的总体目标

企业 AI 权限模型建议同时满足六个目标：

1. **身份可映射**：任何 Agent 行为都必须能映射到真实责任主体  
2. **权限可继承但不越权**：AI 可以代表用户执行，但不能超越用户边界  
3. **动作可分级**：不同风险等级的工具调用采用不同控制强度  
4. **授权可临时化**：针对一次会话或单次任务进行临时授权  
5. **审批可门控**：高风险动作必须触发额外审批或人工确认  
6. **结果可追溯**：任何访问与执行都可还原授权依据和执行链路

---

## 三、总体权限架构

```text
┌────────────────────────────────────────────────────────────┐
│                        用户身份层                           │
│ 员工 / 外包 / 管理员 / 审批人 / 审计员 / 系统账号            │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                      身份映射与会话层                        │
│ SSO / MFA / Session / Delegation Token / Context Claim     │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                      Agent 授权决策层                        │
│ 角色策略 / 属性策略 / 风险分级 / 条件策略 / 审批门控         │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                     Tool Gateway 控制层                      │
│ 工具级鉴权 / 参数约束 / 数据脱敏 / 执行确认 / 二次校验       │
└──────────────────────────┬─────────────────────────────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│                      业务系统执行层                          │
│ ERP / OA / LIMS / MES / QMS / DMS / 知识库 / 流程系统        │
└────────────────────────────────────────────────────────────┘
```

---

## 四、建议采用的五层权限模型

### 4.1 L1：身份认证层
关注“你是谁”。

关键要求：
- 接入企业统一身份源（AD / LDAP / IAM / IdP）
- 强制单点登录与多因子认证
- 所有会话携带唯一身份标识
- 建立用户、岗位、部门、组织的主数据映射

### 4.2 L2：角色授权层
关注“你通常能做什么”。

建议方式：
- 按岗位建立标准角色包
- 将 AI 平台角色与企业系统角色解耦
- 通过角色模板快速完成首轮授权
- 对外包、临时人员、项目成员设置独立角色边界

### 4.3 L3：属性与上下文策略层
关注“在当前条件下是否能做”。

关键属性包括：
- 部门 / 站点 / BU
- 数据密级
- 系统域
- 时间窗口
- 网络位置
- 设备可信度
- 当前任务上下文

此层适合采用 ABAC（Attribute-Based Access Control）设计，以解决复杂的跨系统、跨组织、跨数据边界问题。

### 4.4 L4：工具与动作控制层
关注“具体这个工具、这个参数、这个动作是否允许”。

示例：
- 允许 `lims.sample.query`，但禁止 `lims.sample.release`
- 允许查询本部门数据，但禁止导出全量数据
- 允许起草审批单，但禁止直接提交最终审批
- 允许对测试环境执行变更建议，但禁止直接改生产配置

这是 Agent 时代最关键的一层，因为真正的风险都落在工具调用级别。

### 4.5 L5：风险审批与例外控制层
关注“高风险动作是否需要额外门控”。

适用场景：
- 批量导出敏感数据
- 提交影响 GxP 记录的动作
- 发起跨系统写入或自动化任务
- 涉及财务、采购、主数据、质量偏差等关键流程

建议机制：
- 人工确认
- 双人复核
- 工作流审批
- 一次性临时授权
- 紧急例外授权与事后追溯

---

## 五、推荐的授权原则

### 5.1 最小权限原则
默认禁止，按需开放；先开放读取，再开放分析；先开放起草，再开放提交；先开放低风险，再逐步扩展高风险。

### 5.2 用户边界继承原则
Agent 不应拥有独立于用户的无限权限。除平台管理员与系统级自动化场景外，Agent 原则上只能在用户权限边界内代理执行。

### 5.3 能力与数据双维授权原则
同一个工具即使允许调用，也不等于允许访问所有数据。必须同时控制：
- 能做什么动作
- 能访问哪些数据范围

### 5.4 高风险动作显式确认原则
越是不可逆、批量化、影响关键记录的动作，越不能“静默自动执行”。

### 5.5 会话最小暴露原则
临时任务获取的敏感上下文，不应自动扩展到后续无关任务；高敏信息应限定会话作用域和生命周期。

---

## 六、典型授权模式设计

### 6.1 User-Delegated（用户委托型）
适用：个人助手、知识问答、报表分析、起草类场景  
特点：以用户身份边界为上限，最常见、最安全。

### 6.2 System-Scoped（系统代理型）
适用：平台巡检、知识同步、夜间作业、批处理  
特点：使用系统账号执行，但必须限定任务范围、执行窗口和审计责任。

### 6.3 Human-in-the-Loop（人工门控型）
适用：审批、主数据变更、关键记录写入  
特点：AI 负责建议和准备，最终动作由人确认。

### 6.4 Break-Glass（紧急例外型）
适用：重大事故响应、系统应急恢复  
特点：允许临时越过常规策略，但必须双重审批、全程审计、事后复盘。

---

## 七、工具级权限控制建议

每个工具都建议定义以下控制元数据：

```json
{
  "name": "qms.deviation.create",
  "riskLevel": "high",
  "authMode": "user-delegated",
  "requiresConfirmation": true,
  "requiresApproval": true,
  "allowedRoles": ["QA_MANAGER", "QUALITY_SPECIALIST"],
  "allowedDataScope": "site-bound",
  "sensitiveFields": ["product_name", "batch_no"],
  "maxRows": 50,
  "timeWindow": "08:00-20:00"
}
```

建议至少控制以下维度：
- 工具名称
- 风险等级
- 鉴权模式
- 所需角色
- 适用部门 / 站点
- 数据范围
- 是否允许批量
- 是否需要确认
- 是否需要审批
- 是否必须脱敏
- 是否允许自动重试

---

## 八、权限决策流程建议

```text
用户发起请求
   ↓
识别用户身份与当前会话上下文
   ↓
解析目标工具 / 动作 / 数据范围
   ↓
校验角色权限
   ↓
校验属性策略（部门、数据密级、时间、地点等）
   ↓
判断风险等级
   ↓
   ├─ 低风险：直接执行
   ├─ 中风险：执行前确认
   └─ 高风险：审批 / 双人复核 / 限制执行
   ↓
记录审计日志与授权依据
```

---

## 九、与企业 IAM 的协同关系

企业 AI 平台不应成为一套孤立权限系统，而应与现有 IAM 协同：

- **身份来源统一**：统一用户目录与组织架构
- **角色来源协同**：继承主角色包，同时补充 AI 专属策略
- **审批流程协同**：复用现有 ITSM / BPM / 审批平台
- **账号生命周期协同**：入转调离自动同步权限变化
- **审计口径协同**：与 SIEM、审计平台形成统一事件口径

---

## 十、制药 / 高合规行业的特别要求

对于制药、医疗、金融等行业，建议额外增加以下控制：

1. 关键 GxP 记录不得由 AI 独立闭环写入  
2. 涉及质量判断、放行、偏差结论的动作必须人工最终确认  
3. 对验证状态、系统版本、提示词版本、工具版本进行联合留痕  
4. 受控文件、电子记录、电子签名场景应与 Part 11 / Annex 11 要求对齐  
5. 对训练数据、参考知识、提示模板建立变更管理与审批机制

---

## 十一、建设路线建议

### 第一阶段：建立基础身份与角色接入
- 接入 SSO / MFA
- 建立用户与组织映射
- 完成基础角色包设计

### 第二阶段：建立工具级权限控制
- 对高频工具建立元数据标准
- 增加风险等级、确认规则、数据范围控制
- 实现统一授权决策服务

### 第三阶段：建立审批门控与临时授权
- 对高风险动作接入审批流
- 支持单次任务授权与会话授权
- 建立例外授权机制

### 第四阶段：形成持续治理体系
- 定期回顾工具权限清单
- 监控异常调用与越权尝试
- 按角色 / 站点 / 业务域开展权限审计

---

## 十二、结语

Agent 时代的权限问题，本质上不是“多一套菜单权限”，而是“如何在人机协同执行链中保留责任边界、控制边界和审计边界”。

一套成熟的企业 AI 权限模型，应让每一次调用都做到：
- **有身份**
- **有依据**
- **有边界**
- **有门控**
- **有留痕**

这既是安全要求，也是企业 AI 能够真正规模化落地的前提。
