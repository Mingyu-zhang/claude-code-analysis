# Claude Code 架构设计深度解析
## 系统化、工程化思想与 AI 规划建设参考

> 版本：1.0 | 日期：2026-04-02 | 适用场景：企业 AI 能力规划

---

## 一、总纲：Claude Code 是什么

Claude Code 是 Anthropic 推出的**自主代码智能体（Agentic Coding Tool）**，不是普通的代码补全工具。它能够：

- 理解整个代码库上下文
- 自主编辑文件、执行命令、调用工具
- 将复杂任务拆解并委托给子智能体
- 在多轮对话中保持状态连续性

其核心技术从最初 **51 万行 TypeScript** 代码，经过 Rust 重写后提炼为约 **2 万行**——这本身就是最大的设计哲学：**极简内核，可扩展外围**。

---

## 二、核心架构全景图

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层（CLI / REPL）               │
│              claude 命令 + 交互式对话 + 斜杠命令           │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   核心 Agent Loop                         │
│   ┌──────────┐    ┌──────────┐    ┌──────────────────┐  │
│   │ 消息历史  │ ←→ │ LLM 决策 │ ←→ │  工具调用 + 结果  │  │
│   │ (状态)   │    │ callModel│    │  ToolResult 反馈  │  │
│   └──────────┘    └──────────┘    └──────────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
┌────────────────┐ ┌──────────────┐ ┌──────────────────┐
│   工具系统      │ │  权限模型     │ │   上下文管理      │
│ Bash/Read/Write│ │  5级权限控制  │ │ 压缩/隔离/记忆    │
│ Task/WebFetch  │ │  渐进式升级   │ │ CLAUDE.md/Skills │
└────────────────┘ └──────────────┘ └──────────────────┘
         │                                   │
         ▼                                   ▼
┌────────────────────────────────────────────────────────┐
│                   插件生态 + MCP 集成                     │
│       Sub-Agents | Skills | Hooks | MCP Servers         │
└────────────────────────────────────────────────────────┘
```

---

## 三、三大核心技术模块

### 3.1 Agent Loop —— 执行引擎

**本质**：一个工具驱动的对话循环（Tool-Driven Conversation Loop）

```
用户输入
   ↓
[消息追加到历史数组]
   ↓
调用 LLM API（传入系统提示 + 消息历史 + 工具定义）
   ↓
解析助手回复
   ↓
   ├── 包含工具调用 ──→ 权限检查 ──→ 执行工具 ──→ 追加 ToolResult ──→ [继续循环]
   │                              ↓（失败）
   │                         失败作为反馈 ──→ [继续循环，LLM调整策略]
   │
   └── 无工具调用 ──→ 输出文本，结束本轮
```

**关键设计决策：消息即状态**

| 设计项 | 选择 | 原因 |
|--------|------|------|
| 状态管理 | 消息数组（追加式） | 可持久化、可回放、可调试 |
| 终止控制 | LLM 自主决定 end_turn | 无需硬编码退出条件 |
| 失败处理 | 错误作为 ToolResult 反馈 | 不崩溃，让模型自适应调整 |
| 状态恢复 | 整个历史可重建 | 会话可随时中断/恢复 |

---

### 3.2 工具系统 —— 动作层

**三层统一架构**：

```
工具注册表（JSON Schema）
      ↓（契约）
    分发器（路由）
      ↓
   实现层（执行逻辑）
```

**分层工具设计**（从低到高）：

```
┌─────────────────────────────────────────────┐
│  高层工具：Task（子代理）/ WebFetch / Diagnose │  ← 复杂编排
├─────────────────────────────────────────────┤
│  中层工具：Edit / Grep / Glob / TodoWrite     │  ← 专业操作
├─────────────────────────────────────────────┤
│  低层工具：Bash / Read / Write               │  ← 原子操作
└─────────────────────────────────────────────┘
```

**设计哲学**：
- **契约驱动**：Schema 作为 LLM 与实现层之间的契约，语言无关
- **最小权限**：每个工具声明所需最低权限
- **可扩展性**：添加新工具 = 定义 Schema + 实现逻辑 + 一行路由注册

---

### 3.3 权限模型 —— 安全控制层

**五级权限体系**：

```
ReadOnly  <  WorkspaceWrite  <  DangerFullAccess  <  Prompt  <  Allow
   ↑                                                              ↑
最保守                                                          最宽松
```

**渐进式升级逻辑**：

```python
if current_permission >= required:
    allow()
elif gap == 1:           # 仅差一级
    ask_user()           # 询问用户确认
else:                    # 差两级以上
    deny()               # 直接拒绝，无需打扰用户
```

**子智能体安全隔离**：子代理拥有高权限，但**没有用户提示接口**——无法向用户申请升权，自动限制在授权边界内。

---

## 四、上下文工程 —— 最被低估的核心竞争力

Claude Code 的可靠性不来自于模型本身，而来自于**精心设计的上下文工程**。

### 六大上下文支柱：

| 支柱 | 机制 | 作用 |
|------|------|------|
| **Agent** | 主/子代理分工 | 避免上下文混合污染 |
| **查询增强** | 模糊输入 → 精确任务 | 降低歧义，提升成功率 |
| **检索** | CLAUDE.md + Skills | 结构化知识按需注入 |
| **提示工程** | 动态加载 + 位置控制 | 高注意力区放关键信息 |
| **记忆** | 四层记忆体系 | 跨会话状态连续性 |
| **工具** | MCP-S CLI 范式 | 上下文高效、协议驱动 |

### 四大失败模式（必须规避）：

```
1. 上下文污染 —— 错误结果在智能体间传播
2. 上下文分心 —— 过度依赖历史导致跑偏
3. 上下文混淆 —— 不相关信息误导决策
4. 上下文冲突 —— 矛盾信息导致行为不一致
```

### 上下文压缩策略：

- **microcompact**：轻量压缩，保留关键决策
- **autocompact**：自动触发，上下文超限时启动
- **snip**：精准裁剪，移除低价值消息
- **子代理隔离**：探索任务在独立上下文中运行，不污染主线

---

## 五、多智能体架构 —— 并发协作范式

### 架构模式：

```
主智能体（Orchestrator）
    │
    ├── Task 工具 ──→ 子代理 A（代码审查）── 独立上下文
    │                                      └── 独立工具集
    │
    ├── Task 工具 ──→ 子代理 B（前端开发）── 独立上下文
    │                                      └── 独立工具集
    │
    └── Task 工具 ──→ 子代理 C（测试执行）── 独立上下文
                                           └── 独立工具集
```

### 子智能体设计三要素：

```markdown
---
name: code-reviewer          # 唯一标识
description: 专家代码审查员    # 委托触发条件（关键！）
tools: Read, Grep, Glob      # 最小权限工具集
model: sonnet                # 任务匹配的模型规格
---
系统提示词（专业领域知识）
```

### 关键约束：
- 子代理**不能再生成子代理**（防递归爆炸）
- 子代理在**独立工作树**中运行（git worktree 隔离）
- 委托由 LLM 根据 `description` 自动判断，无需硬编码路由

---

## 六、工程化设计哲学六大铁律

### 铁律一：可调试性 > 架构炫技
> 保持单一主循环，最多一层分支（子任务）。控制流清晰是可维护性的前提。

**企业应用建议**：任何 AI 系统先问"出了问题怎么排查"，再问"能实现什么功能"。

---

### 铁律二：小模型大闭环（成本工程）
> 80% 的读/扫/总结任务用小模型（如 Haiku），只在关键生成步骤用大模型。

| 任务类型 | 推荐模型 | 成本比例 |
|---------|---------|---------|
| 内容摘要、格式化 | Haiku（小模型） | 10% 成本 |
| 代码搜索、文件读取 | Haiku | 10% 成本 |
| 代码生成、复杂推理 | Sonnet/Opus（大模型） | 100% 成本 |

**效果**：总成本降低 70-80%，响应速度提升 3-5 倍。

---

### 铁律三：显式任务清单（TodoWrite 机制）
> 让模型将计划可视化，持续维护 TODO 清单，防止长会话中的"目标漂移"。

```
用户请求 → 模型制定计划（写入 TODO）→ 逐步执行 → 实时更新状态
                                                 ↑
                                        每步检查是否偏离目标
```

---

### 铁律四：LLM Search 优于 RAG（简单胜过复杂）
> 用 ripgrep/find/jq + LLM 搜索代码库，比复杂向量 RAG 更透明、可调试。

**适用原则**：先用最简单的方案，只在简单方案不够时引入复杂机制（如向量检索）。

---

### 铁律五：上下文文件即活文档（CLAUDE.md）
> 项目规范、团队约定、忽略目录写入 CLAUDE.md，成为 AI 的"单一事实来源"。

```markdown
# CLAUDE.md 结构示例
## 项目概述
## 团队编码规范
## 禁止修改的目录
## 常用命令速查
## 关键决策记录
```

---

### 铁律六：失败即反馈（Fail-as-Feedback）
> 工具执行失败不崩溃，错误结果作为 ToolResult 反馈给模型，让 LLM 自适应调整策略。

**系统韧性设计**：错误是信息，而非终点。

---

## 七、MCP（模型上下文协议）—— 开放生态基础

MCP（Model Context Protocol）是 Claude Code 的**外部扩展总线**，定义了统一的工具接入标准。

```
Claude Code 核心
      ↕ MCP 协议
┌─────────────────────────────────────────┐
│  MCP Servers（外部工具提供方）            │
│  ├── 数据库查询工具                       │
│  ├── API 调用工具（CRM/ERP/LIMS 等）     │
│  ├── 文件系统工具                        │
│  └── 自定义业务工具                      │
└─────────────────────────────────────────┘
```

**核心价值**：
1. **标准化接入**：所有外部工具遵循统一协议，降低集成成本
2. **权限隔离**：每个 MCP Server 独立权限控制
3. **动态发现**：运行时发现可用工具，无需重新编译

---

## 八、对企业 AI 规划建设的启示

### 8.1 架构规划层面

| Claude Code 设计 | 企业 AI 建设借鉴 |
|----------------|----------------|
| 极简核心 + 可扩展外围 | AI 平台核心能力内聚，业务能力插件化 |
| 消息即状态 | AI 交互历史即业务状态，可审计可回放 |
| 子代理隔离 | 不同业务域的 AI 独立部署，互不干扰 |
| 权限渐进升级 | AI 能力开放需分级授权，不能全开 |
| MCP 标准化 | 建立企业内部 AI 工具标准接口规范 |

### 8.2 能力建设层面

**第一阶段：基础能力**
- [ ] 建立企业 AI 工具标准接口（参考 MCP）
- [ ] 完成主要业务系统的 AI Tool 封装
- [ ] 搭建基础 Agent Loop 执行引擎

**第二阶段：上下文工程**
- [ ] 建立企业知识库（对应 CLAUDE.md + Skills）
- [ ] 实现上下文压缩和记忆管理
- [ ] 建立 AI 行为监控和可观测性

**第三阶段：多智能体协作**
- [ ] 设计业务领域专属子智能体
- [ ] 实现并行任务编排
- [ ] 建立智能体协作的治理规范

### 8.3 关键风险提示

```
⚠️ 常见陷阱                    ✅ 正确做法
─────────────────────────────────────────────
上来就用大模型干所有事           → 小模型处理简单任务，节省成本
过早引入复杂 RAG 体系            → 先用简单搜索，验证需求再扩展
多智能体过于复杂，调试困难        → 保持单主循环，只在必要时分叉
权限控制过宽或过严              → 渐进式权限，差一级才提示用户
上下文不加管理，越来越长          → 主动压缩 + 子代理隔离
```

---

## 九、总结：设计思想的本质提炼

### 三个核心思想

```
┌────────────────────────────────────────────────────────┐
│  1. 简单性优先（Simplicity First）                      │
│     核心逻辑 88 行代码，复杂性来自扩展层而非核心          │
├────────────────────────────────────────────────────────┤
│  2. 可观测性驱动（Observability-Driven）                │
│     可调试 > 功能炫技，消息即状态，失败即信息             │
├────────────────────────────────────────────────────────┤
│  3. 渐进式开放（Progressive Exposure）                  │
│     权限渐进、能力渐进、复杂度渐进，每一步都可控可回退      │
└────────────────────────────────────────────────────────┘
```

### 一句话总结

> **Claude Code 的架构哲学是：用极简的核心执行引擎（Agent Loop），通过标准化工具契约（Tool Schema），在精心管理的上下文窗口中，实现可观测、可扩展、可治理的自主智能行为。**

这套哲学完全适用于企业 AI 规划建设——无论是制药行业的 GxP 合规 AI，还是通用的业务流程 AI 化，核心都是：**先让 AI 可控可信，再让 AI 能力扩张。**

---

*参考来源：*
- *DeepWiki - Claude Code System Architecture*
- *DEV.to - Claude Code Architecture Explained (Rust Rewrite Analysis)*
- *ClaudeFast - Context Engineering: The Six Pillars*
- *Claude Code 官方文档 - Sub-Agents*
- *Claude 中文社区 - Agent Loop 深度解读*
