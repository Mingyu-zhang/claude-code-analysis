# GxP 场景下 AI 验证方法
## 面向制药 / 生物医药企业的 AI 系统验证、变更与持续合规设计

> **文档类型**：专题方法白皮书  
> **版本**：1.0  
> **发布日期**：2026-04-02  
> **适用对象**：QA、RA、CSV、IT、数字化团队、系统负责人、审计与合规团队  
> **核心主题**：验证边界、风险分级、测试策略、变更控制、持续验证、审计证据

### 摘要
AI 系统进入 GxP 场景后，最难的问题不是“模型是否先进”，而是“如何证明它在受控边界内可用、可控、可追溯”。相比传统计算机化系统，AI 系统具有概率性输出、上下文敏感、知识动态变化、工具链依赖复杂等特征，因此不能简单照搬传统 CSV（Computerized System Validation）方法，而需要形成“**风险分级 + 边界验证 + 场景测试 + 变更控制 + 持续监测**”的组合型方法。

本文提出一套适用于制药 / 生物医药企业的 GxP 场景 AI 验证方法，用于帮助企业在确保质量与合规的前提下，推进 AI 辅助问答、文档起草、偏差分析、知识检索和流程辅助等场景的落地。

---

## 一、为什么 AI 验证不同于传统 CSV

传统 CSV 更适用于逻辑确定、输入输出相对固定的系统，而 AI 系统具有以下差异：

1. **输出具有概率性**：同一问题在不同上下文下可能给出不同表述  
2. **质量依赖外部要素**：提示词、知识库、工具接口、模型版本都会影响结果  
3. **边界更难定义**：系统既可能只是问答助手，也可能串联执行多个动作  
4. **错误形态更多样**：不仅有系统错误，还有幻觉、误引、越权、误操作等风险  
5. **更新频率更高**：提示模板、知识文档、模型路由、工具版本可能经常变化

因此，AI 验证不能只问“系统是否按规格运行”，还必须问：
- 它在什么边界内被认为合格？
- 哪些风险是可接受的？
- 发生偏差后是否可追溯、可纠正？

---

## 二、GxP 场景下的验证目标

建议将 AI 系统验证目标定义为五类：

1. **功能适用性**：在定义场景内是否能提供可接受的辅助结果  
2. **边界可控性**：是否明确哪些事情可以做、哪些不能做  
3. **合规可追溯性**：是否保留完整的审计链、版本链、变更链  
4. **风险可管理性**：关键风险是否已识别、控制、监控  
5. **持续可维护性**：系统更新后是否仍能保持已验证状态

---

## 三、建议的验证对象分层

AI 系统验证不应只验证“模型”，而应分层验证整个能力链：

### 3.1 业务场景层
- 偏差辅助分析
- SOP / WI 检索问答
- 培训问答助手
- 记录草稿生成
- 审批材料辅助整理

### 3.2 应用流程层
- 用户输入处理
- Prompt 编排
- 知识检索
- 工具调用
- 输出审查
- 人工确认

### 3.3 技术组件层
- 模型网关
- RAG 检索服务
- Tool Gateway
- 权限引擎
- 审计服务
- 版本管理与配置中心

### 3.4 受控资产层
- 提示模板
- 受控知识文档
- 规则库
- 工具配置
- 风险控制参数

---

## 四、基于风险的验证分类建议

建议对 AI 场景进行风险分级：

| 等级 | 场景示例 | 风险特征 | 验证强度建议 |
|------|----------|----------|--------------|
| V1 | 培训问答、公开知识查询 | 低业务影响 | 基础功能验证 + 样例回归 |
| V2 | 内部文档起草、信息整合 | 中等业务影响 | 场景测试 + 输出抽检 |
| V3 | 涉及 GxP 辅助分析 | 高业务影响 | 强化风险评估 + 边界测试 + 审计验证 |
| V4 | 涉及质量判断或关键记录 | 极高业务影响 | 原则上不允许 AI 自动闭环；必须人工主导 |

核心原则：**风险越高，越强调边界控制、人工复核与持续监测，而不是单纯增加测试用例数量。**

---

## 五、验证策略设计

### 5.1 边界验证
验证系统明确拒绝不允许的请求，例如：
- 越权访问
- 自动做出质量结论
- 直接写入关键记录
- 引用未授权知识源

### 5.2 场景验证
围绕具体业务用例验证输出质量与稳定性，例如：
- 是否能正确引用受控文件
- 是否能给出完整且不误导的步骤建议
- 是否能在缺失信息时提示用户补充

### 5.3 工具链验证
验证 Agent 在调用工具时：
- 权限是否正确生效
- 参数是否符合限制
- 错误是否可处理
- 高风险动作是否触发人工确认

### 5.4 审计验证
验证系统是否能完整记录：
- 谁发起了请求
- 使用了哪个 Prompt 版本
- 调用了哪些工具
- 参考了哪些知识文档
- 最终输出和审批链是什么

### 5.5 回归验证
当模型、提示词、知识库或工具发生变化时，验证关键场景是否仍保持可接受表现。

---

## 六、测试用例设计建议

AI 测试不宜只采用传统“输入 - 期望唯一输出”方式，而建议组合以下方法：

### 6.1 黄金样例集（Golden Set）
建立一组代表性业务问题，定义：
- 必须覆盖的要点
- 不允许出现的错误
- 可接受的表述范围

### 6.2 风险样例集（Risk Set）
专门测试：
- 幻觉
- 越权
- 敏感信息泄露
- 工具误用
- 未经确认的高风险动作

### 6.3 负向样例集（Negative Set）
测试系统是否会在不该回答、不该执行时正确拒绝。

### 6.4 回归样例集（Regression Set）
在每次版本更新后重复执行，确保关键能力与控制机制未退化。

---

## 七、建议形成的验证文档包

对于进入 GxP 管理范围的 AI 场景，建议至少形成以下文档：

- 业务需求说明（URS）
- 功能 / 风险规格说明
- 风险评估记录
- 验证计划（VP）
- 测试方案与测试记录
- 偏差与 CAPA 记录
- 发布审批记录
- 变更控制记录
- 持续监测与周期回顾记录

如果企业已有 CSV 模板，可将 AI 特有控制项嵌入现有模板中，而不是完全另起炉灶。

---

## 八、变更控制建议

AI 系统的“变更”不仅包括代码变更，还包括：
- 模型版本切换
- 提示模板调整
- 知识库内容更新
- 工具接口新增 / 变更
- 权限规则调整
- 审计策略调整

建议按变更影响分为三类：

### C1：低影响变更
如 UI 微调、非关键提示文案优化。  
可采用简化评估 + 抽样回归。

### C2：中影响变更
如知识库范围更新、非关键工具逻辑调整。  
需开展影响评估 + 重点回归测试。

### C3：高影响变更
如模型切换、权限策略变化、关键场景 Prompt 结构调整。  
应进行正式变更评估、审批及增强验证。

---

## 九、持续验证与运行期监测

AI 系统通过初始验证并不代表永久合格，建议建立持续验证机制：

- 周期性抽检输出质量
- 监控错误类型分布与趋势
- 监控知识失效与引用偏差
- 监控越权与敏感访问异常
- 对关键场景进行定期再验证
- 建立用户反馈与纠错闭环

持续验证的重点是：**确保系统在环境变化、知识变化、模型变化后仍保持受控状态。**

---

## 十、与法规与指南的关系

企业在设计 AI 验证方法时，可参考以下框架精神：

- GAMP 5 风险基础方法
- 21 CFR Part 11 对电子记录 / 电子签名要求
- EU GMP Annex 11 对计算机化系统要求
- 数据完整性 ALCOA+ 原则
- 企业内部 CSV / 变更控制 / 偏差管理 SOP

关键点不在于“AI 是否有专门新法规”，而在于能否把现有质量与验证原则合理映射到 AI 系统。

---

## 十一、推荐建设路线

### 第一阶段：从低风险场景起步
- 先从培训问答、知识检索、文档辅助开始
- 明确系统边界和禁区
- 建立基础日志与人工复核机制

### 第二阶段：建立验证框架
- 建立黄金样例集和风险样例集
- 建立 Prompt / 知识 / 工具版本台账
- 建立标准验证模板

### 第三阶段：形成持续验证机制
- 接入运行期监测与审计
- 建立定期回顾
- 将 AI 纳入正式变更控制体系

### 第四阶段：支撑高价值高合规场景
- 在充分边界控制和人工门控前提下，扩展到偏差辅助、调查辅助、受控文档辅助等场景
- 对关键场景实施专项验证与定期再确认

---

## 十二、结语

GxP 场景中的 AI 验证，核心不是证明“AI 永远不会错”，而是证明：
- 它的用途边界是清楚的
- 它的风险是被识别和控制的
- 它的行为是可追溯和可审查的
- 它的变化是受控的
- 它在关键节点有人类把关

这才是 AI 能够真正进入高合规业务场景的前提。
